const userStatusEnum = Object.freeze({
    PENDING: 'Pending',
    VERIFIED: 'Verified',
})

const userRoleEnum = Object.freeze({
    ADMIN: 'Admin',
    USER: 'User',
})

module.exports = {
    userStatusEnum,
    userRoleEnum
}